---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "gitea_label Resource - terraform-provider-gitea"
subcategory: ""
description: |-
  This resource manages labels for repositories or organizations in Gitea.
---

# gitea_label (Resource)

This resource manages labels for repositories or organizations in Gitea.

## Example Usage

```terraform
resource "gitea_label" "repo_label" {
  user        = "my-org"
  repo        = "my-repo"
  name        = "bug"
  color       = "#ff0000"
  description = "Something isn't working"
}

resource "gitea_label" "org_label" {
  org         = "my-org"
  name        = "feature"
  color       = "#00ff00"
  description = "New feature request"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `color` (String) 6-character hex color code (e.g. `ff0000` or `#ff0000`)
- `name` (String) The name of the label

### Optional

- `description` (String) Description of the label
- `exclusive` (Boolean) Whether the label is scoped/exclusive
- `org` (String) Organization name to create an org-level label
- `repo` (String) Repository name to create a repo-level label
- `user` (String) User or organization owner of the repository

### Read-Only

- `id` (String) The ID of this resource.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Using `import` blocks in Terraform v1.5.0 and later:
# import {
#   to = gitea_label.repo_label
#   id = "<user>/<repo>/<id>"
# }

# Using `terraform import` in Terraform v1.4.0 and earlier:
terraform import gitea_label.repo_label <user>/<repo>/<id>
```
